<dic class="container">
	<img {{bind-attr src=this.profilePic}} /><br />
	Theme Pic: {{themePic}} <br />
	Member Since: {{memberSince}} <br />
	First Name: {{firstName}} <br />
	Last Name: {{lastName}} <br />
	Photos
	{{#each photo in controller.photos}}
		{{photo.title}}
	{{/each}}
</div>